apiVersion: v1
kind: Service
metadata:
  name: monitoring-dashboard
  namespace: monitoring
  labels:
    app: monitoring-dashboard
spec:
  type: LoadBalancer
  ports:
  - port: 9493
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: monitoring-dashboard
