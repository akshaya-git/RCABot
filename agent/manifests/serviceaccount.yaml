apiVersion: v1
kind: ServiceAccount
metadata:
  name: monitoring-agent-sa
  namespace: monitoring
  annotations:
    # This will be populated by Terraform with the IRSA role ARN
    eks.amazonaws.com/role-arn: "${MONITORING_AGENT_ROLE_ARN}"
  labels:
    app: monitoring-agent
